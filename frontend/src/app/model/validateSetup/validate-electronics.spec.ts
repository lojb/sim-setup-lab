import { ValidateElectronics } from './validate-electronics';

describe('ValidateElectronics', () => {
  it('should create an instance', () => {
    expect(new ValidateElectronics()).toBeTruthy();
  });
});
