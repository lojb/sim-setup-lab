import { ValidateFuelStrategy } from './validate-fuel-strategy';

describe('ValidateFuelStrategy', () => {
  it('should create an instance', () => {
    expect(new ValidateFuelStrategy()).toBeTruthy();
  });
});
